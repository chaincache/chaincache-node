{
  "name": "chaincache-node",
  "version": "0.0.1",
  "description": "ChainCache API client for Node.js",
  "main": "src/index.js",
  "engines": {
    "node": ">=8.10.0"
  },
  "dependencies": {
    "axios": "0.19.0"
  },
  "devDependencies": {
    "mocha": "6.1.4",
    "should": "13.2.3"
  },
  "scripts": {
    "test": "npx mocha --exit --bail --slow 1000 --timeout 5000 ./test"
  }
}
